def removeDuplicates(arr,n):
    # brute force method

     s = set()
     for i in range(n):
         s.add(arr[i]) 

     idx =0 
     for i in s:
         idx += 1
     return idx

	or 

def removeDuplicates(arr,n):
    i = 0
    for j in range(1,len(arr)):
        if arr[j] != arr[i]:
            arr[i+1] = arr[j]
            i += 1
    return i+1
    pass	
